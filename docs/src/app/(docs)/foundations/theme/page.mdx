import TokenSwatches from "@/components/docs/TokenSwatches";

# Themes

js-ds-ui ships with three built-in themes: light, dark, and high-contrast. Themes
set semantic color tokens such as `--color-text-primary` and
`--color-background-primary` so components remain accessible across modes.

## Live swatches

<TokenSwatches />
