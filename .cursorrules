# Cursor entry point

The single source of truth for agent instructions in this repository is `AGENTS.md`.

Read `AGENTS.md` first, then consult the relevant files under `.agents/rules/` and `.agents/skills/` based on the file or task at hand. Optional rules under `.agents/rules/optional/` apply only when listed in the "Enabled optional rules" section of `AGENTS.md`.
