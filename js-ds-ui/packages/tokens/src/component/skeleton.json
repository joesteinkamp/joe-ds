{
  "$description": "Component-specific tokens for Skeleton",
  "component": {
    "skeleton": {
      "bg": { "$type": "color", "$value": "{color.background.tertiary}", "$description": "Skeleton loading placeholder background" },
      "border": {
        "radius": { "$type": "dimension", "$value": "0.5rem", "$description": "Skeleton border radius" }
      }
    }
  }
}
