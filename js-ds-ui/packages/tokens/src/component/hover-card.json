{
  "$description": "Component-specific tokens for HoverCard",
  "component": {
    "hover-card": {
      "bg": { "$type": "color", "$value": "{color.background.primary}", "$description": "Hover card background" },
      "text": { "$type": "color", "$value": "{color.text.primary}", "$description": "Hover card text color" },
      "border-color": { "$type": "color", "$value": "{color.border.default}", "$description": "Hover card border color" },
      "padding": { "$type": "dimension", "$value": "{space.component.padding.md}", "$description": "Hover card padding" },
      "width": { "$type": "dimension", "$value": "16rem", "$description": "Hover card default width" },
      "border": {
        "radius": { "$type": "dimension", "$value": "0.375rem", "$description": "Hover card border radius" }
      }
    }
  }
}
