{
  "$description": "Component-specific tokens for Popover",
  "component": {
    "popover": {
      "bg": { "$type": "color", "$value": "{color.background.primary}", "$description": "Popover background color" },
      "text": { "$type": "color", "$value": "{color.text.primary}", "$description": "Popover text color" },
      "border-color": { "$type": "color", "$value": "{color.border.default}", "$description": "Popover border color" },
      "padding": { "$type": "dimension", "$value": "{space.component.padding.lg}", "$description": "Popover content padding" },
      "width": { "$type": "dimension", "$value": "18rem", "$description": "Popover default width" },
      "border": {
        "radius": { "$type": "dimension", "$value": "0.375rem", "$description": "Popover border radius" }
      }
    }
  }
}
